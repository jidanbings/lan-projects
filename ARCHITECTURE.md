# lan-projects 架构说明

> **一句话：一个"手机即服务器"的局域网文件共享工具。** Web 前端 + 内嵌 Node.js 的 Android App + 桌面浏览器，全部在局域网内传输，端到端加密，数据不出网、不上云。

---

## 1. 项目定位

跨平台的 AirDrop 替代方案，解决"局域网内多台设备互相传文件"：

- **无账号、无云端、无公网依赖**——同一局域网内即可用；
- **一台设备就能当服务器**（Android App 内嵌 Node.js），其他设备用浏览器扫码/输码连接；
- **三端共用同一份前端代码**，行为一致；
- **运行要求**：Android App 需 **Android 9.0（API 28）及以上 + 64 位 ARM（arm64-v8a）**（`targetSdk 35` 不是版本上限，Android 15 / 16 实测可用，更高版本向后兼容）；电脑端需 **Node.js ≥ 18**（Windows / macOS / Linux 均可，Windows 双击 `start.bat` 即可）。

---

## 2. 系统组成

| 端 | 运行载体 | 关键特点 |
|---|---|---|
| **Android App** | 系统 WebView + 内嵌 Node.js 运行时 | 手机即服务器；WebView 不支持 WebRTC，传输走 **WebSocket 中继** |
| **PC / Mac 浏览器** | 桌面浏览器 | 直接访问局域网 IP；支持 **WebRTC P2P 直连**（不过服务器中继） |
| **桌面服务器包** | 本地 Node.js | 电脑端一键启动服务器，供手机/浏览器连接 |

**共享代码原则：`web/` 是唯一代码源。** Android App 与电脑端跑的是同一份前端代码，改一处，三端生效。

---

## 3. 运行时架构（一次传输的全貌）

```
┌─────────────┐   WebSocket    ┌──────────────────────────┐   WebSocket   ┌─────────────┐
│  发送方设备  │ ─────────────▶ │  服务器设备（内嵌 Node.js） │ ────────────▶ │  接收方设备  │
│  WebView/浏览器 │  加密的1MB二进制帧 │ 信令 + WS 中继（不解密）   │   原样转发    │  WebView/浏览器 │
└─────────────┘                └──────────────────────────┘               └─────────────┘
        ▲                              │                                         │
        └──── 高熵配对码 S / QR 离网交换（服务器永远不见 S）◄────── 扫码/输码 ─────────┘
```

1. **一台设备 = 服务器**：运行 Node.js，提供 HTTP 静态页面 + WebSocket 信令/中继；
2. **其他设备 = 客户端**：浏览器 / WebView 通过 WebSocket 连接服务器；
3. **设备发现（IP 房间）**：连到同一服务器的所有设备，加入一个以"服务器局域网地址"为 ID 的**共享房间**，彼此可见；
4. **配对（零信任）**：发起方生成 **128 位随机密钥 S**（base32 编码为 26 字符），**只在离网渠道传递**（二维码 / 手动输入），服务器永远接触不到 S；双方用 HKDF 从 S 派生房间路由 id R 与会话密钥，各自凭 R 进入私有"秘密房间"；
5. **传输**：发送方读取文件 → 端到端加密（AEAD，含元数据） → 切 1MB 二进制帧 → 服务器原样转发 → 接收方解密 → 写盘。

---

## 4. Android App 的双进程设计

```
┌────────────── Android 进程 ──────────────┐
│  UI 进程：Activity + WebView + 写盘服务    │
│   └ HostSessionService（前台服务：WakeLock │
│      + WifiLock，传输会话期间保活）        │
│      │                                   │
│      ▼                                   │
│  :node 进程（独立进程）：Node.js 服务器    │
└──────────────────────────────────────────┘
```

- **`:node` 进程跑 Node.js**（前台服务）。必须与 UI 进程隔离：V8 的 wasm trap-handler 与 WebView 的 Chromium 崩溃处理器冲突（Android 15/16 会 SIGTRAP），独立进程是修复方案；
- **回主页 = 杀掉 `:node` 进程** = 停服务器。不做后台保活，退出即断，符合"像划掉进程一样"的产品意图；
- **传输会话期间，主进程由 `HostSessionService`（前台服务）保活**：它同时持有 **WakeLock + WifiLock** 并在传输页整页保持屏幕常亮（`FLAG_KEEP_SCREEN_ON`）。原因：主进程承载 WebView/WebSocket，却没有前台组件——用户打开系统文件选择器选文件的瞬间 App 就被后台化，主进程可能被系统冻结/杀死，连接随之断开（"收文件的手机自动断开"）。保活服务让主进程在会话期间留在前台服务桶，选文件 / 息屏都不会掐断连接；离开传输页（`onDestroy`）即停止。
- WebView 加载的是打进 assets 的同一份 web 前端，与桌面浏览器完全同源。

---

## 5. 文件传输链路（分步）

| 环节 | 实现 | 为什么 |
|---|---|---|
| 读取 | `FileReader` 按 **1MB** 分块 | 实测 1MB 在真机上是吞吐甜点 |
| 加密 | **ChaCha20-Poly1305 AEAD**（纯 JS，noble-ciphers） | 加密 + 完整性认证一体（TLS 1.3 同款）；`crypto.subtle` 在 `http://局域网IP` 不可用，只能纯 JS；比纯 JS AES 快约 12 倍 |
| 元数据 | 文件名/大小/分块偏移/聊天文本**一并 AEAD 加密**，服务器只见密文与明文路由字段 | 中毒服务器看不到"传了什么文件、多大" |
| 分帧 | 1MB **二进制 WebSocket 帧** + `ws-chunk-prep` 宣告路由 | 二进制帧免 base64 开销（否则被卡在 ~5MB/s） |
| 中继 | 服务器按宣告的路由**原样转发**加密字节，不解密 | 服务器不碰明文，保持端到端 |
| 写盘 | 手机走 **SaveFileServer**（127.0.0.1:3900）原生二进制写盘 | 替代"base64 过 JS 桥"：1GB 从 ~20 秒降到 ~3 秒 |
| 直连 | 桌面 ↔ 桌面可走 **WebRTC DataChannel** P2P | 不占服务器带宽；Android WebView 无 WebRTC，故回退 WS |
| 保活 | 客户端 3s 一次 JSON ping（9s 无 pong 判定服务端已消失）+ 服务端协议级 ping 帧（保活预算 ~10 分钟） | 热点链路常不会把 TCP FIN 送到对方，客户端主动探测服务端死活；服务端放宽保活预算避免误踢藏在文件选择器后的健康设备 |

**速度上限**：传输吞吐 = 发送端手机 JS 处理能力（FileReader + 加密 + 分帧）。好手机 ~20MB/s，弱手机 ~5MB/s；局域网本身约 300MB/s。App 内置"速度传输"页可实测本机上限。

---

## 6. 安全模型

> **核心威胁模型：服务器设备失陷。** 当"作为服务器的手机中病毒"，攻击者获得了服务器上的全部控制权——旧设计里服务器手握密钥，中毒即全盘泄露。本次改造让**一台被攻破的服务器既解不开、也改不动**。

| 维度 | 做法 |
|---|---|
| **数据不出局域网** | 服务器只监听局域网地址，不监听公网接口 |
| **零信任密钥协商** | 会话密钥 **S**（128 位随机，base32 编码为 26 字符）**只在离网渠道建立**（二维码 / 手动输入），服务器永远不见 S。服务器只拿到 HKDF 派生的房间路由 id R，无法从 R 反推 S 或会话密钥 |
| **端到端加密 + 完整性** | 密钥 = `HKDF-SHA256(S)`，加密 = **ChaCha20-Poly1305 AEAD**（机密性 + 完整性认证一体）；任一字节被改（含服务器篡改）→ 认证失败 → 丢弃并提示。未配对设备**拒绝传输** |
| **元数据加密** | 文件名 / 大小 / 分块偏移 / 聊天文本随文件内容一并 AEAD 加密；服务器只见密文与明文路由字段，看不到"传了什么、多大" |
| **配对确认握手** | 双方入房后各发一条 AEAD 加密的握手包，解密失败 → "密钥不一致"，不进入可传输状态；恶意服务器把加入方路由到错误配对也会在此快速失败 |
| **前端完整性锚点（Android）** | 构建期把 `public/**` 逐文件 SHA-256 写入 APK 资产清单（随 APK 签名而不可变）；`NodeService` 每次启动对解包目录**双向校验**（数量 + 哈希），任一不符 → **fail-closed，不启动服务器**并提示用户。非 root 病毒改不了签名保护的 APK，就换不了客户端手里的加解密代码 |
| **扫码连接白名单** | 扫码只接受局域网服务器的 http 连接 / 配对 / 房间码（私网 IP、IPv6 本地、`.local` 主机名），公网 https / 公网 IP / 任意路径一律拒绝——恶意二维码无法把 App 导向钓鱼网站 |
| **WebView 导航隔离** | 指向公网的链接或重定向（含服务端 302）自动改用**系统浏览器**打开；非局域网页面永远进不了 App 的 WebView 主框架 |
| **文件写盘双保险** | JS 桥 `LanProjectsBridge` 与本地写盘服务 `SaveFileServer`（127.0.0.1:3900）都校验来源 origin：仅局域网 lan-projects 页面可写「下载」目录，混入的恶意页面即使调用接口也被拒绝 |
| **防克隆/重打包** | 官方 APK 运行时校验签名 SHA-256，克隆包直接拒启 |
| **日志隐私** | 日志只留最近 100 条，存于应用私有目录，可在 App 内查看/清空；崩溃日志 crash.log 设 512KB 上限，超限自动轮转，崩溃循环不会撑爆数据 |

**残余边界（文档明示）**：
- **root / 彻底沦陷可绕过一切**：攻击者一旦拿到 root，可改写应用自身（Java 层、Node 运行时、签名后的 APK 内容），任何应用内校验都失效——这是所有终端应用的安全下限；
- **纯浏览器客户端没有独立锚点**：浏览器端加解密代码仍来自服务器（无 SRI 锚点），若服务器被攻破，浏览器用户拿到的可能是被替换过的代码。**敏感传输请使用官方签名 App**（有完整性锚点）；
- **完整性锚点覆盖 `public/**` 前端**：服务器自身 JS（`server/`）不在校验范围——服务器代码本来就运行在"攻击者"手里，锚点防不住这个，前端锚点的价值是保护**客户端**拿到真代码。

---

## 7. 关键设计决策回顾

| 决策 | 原因 |
|---|---|
| Node 跑独立 `:node` 进程 | 避免 Android 15/16 下 V8 与 WebView 的 SIGTRAP 原生崩溃 |
| WebView 复用 web 前端 | 一套代码跑三个端，改一处三端生效 |
| 手机传输走 WS 中继 | Android 系统 WebView 不支持 `RTCPeerConnection` |
| 密钥协商移出服务器（零信任） | 服务器手机可能中毒，密钥绝不能再由服务器生成/分发；S 只在离网渠道交换，服务器只见 HKDF 派生的房间 id |
| 高熵配对码（128 位 base32，26 字符） | 6 位数字码熵太低，暴力可试破；26 字符 base32 无法靠试错攻破，且可防"服务器下发假密钥" |
| 加密用 ChaCha20-Poly1305 AEAD | 机密性 + 完整性认证一体（TLS 1.3 同款）；非 HTTPS 局域网来源无 `crypto.subtle`，只能纯 JS；比纯 JS AES 快约 12 倍 |
| 元数据一并 E2E 加密 | 文件名/大小/偏移/聊天文本若不加密，中毒服务器能看到"传了什么文件" |
| Android 前端完整性锚点（构建期清单 + 启动期双向校验，fail-closed） | 加解密代码由服务器下发，必须让客户端只能拿到验真代码；APK 签名保护的清单是可信参考，非 root 病毒改不动 |
| 收文件用 SaveFileServer 原生写盘 | 免 base64 过 JNI 桥，1GB 从 ~20s 降到 ~3s |
| 文件块用二进制 WS 帧 | 去掉 base64 33% 开销与 atob/btoa，突破 5MB/s 上限 |
| 日志 100 条上限 + crash.log 512KB 轮转 | 防止日志无限增长 |
| 传输会话期 HostSessionService 保活（WakeLock + WifiLock + 整页屏幕常亮） | 选文件时 App 被后台化，没有前台组件的主进程可能被系统冻结/杀死导致断连；保活服务让连接在选文件 / 息屏期间不断 |
| 客户端 3s 心跳 + 服务端协议级 ping | 热点链路半开时客户端能主动发现服务端已消失；服务端用浏览器网络栈自动应答的 ping 帧保活，页面冻结也不被误踢 |
| 更新包安装完成后自动删除 | 系统安装器不会删下载的 APK，每次更新都会在 App 数据里留一个 ~30-50MB 安装包；启动时按版本号自动清理 |
