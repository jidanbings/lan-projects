<?xml version="1.0" encoding="utf-8"?>
<!-- The in-app server and LAN peers use plain HTTP, so cleartext must be allowed. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
