<?xml version="1.0" encoding="utf-8"?>
<!-- White card behind the LAN status / address block, so the IP stands out on
     the light gray page background (matches the settings page's light look). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" />
    <corners android:radius="16dp" />
    <stroke android:width="1dp" android:color="#D9DEE3" />
</shape>
