## [1.0.103 ~ 1.0.124] - 2026-08-04

### 修复
- **「选择发送文件」长时间停留不再断联**：以前打开系统文件选择器选文件，一旦停留超过 10~35 秒，收发双方会同时掉线（文件选择器打开时 App 自身界面被暂停、屏幕息屏后 WiFi / 热点无线电进入省电，把两台手机间的链路掐断）。现在传输会话期间持有 **WiFi 锁**（连同原有的 CPU 锁 / 前台服务），息屏也不让无线电休眠；同时前端在页面隐藏时不再因网络质量波动误关连接、返回页面后若连接卡在半路会自动重建
- **选文件期间强制不熄屏，根治热点断连**：之前服务端手机打开文件选择器、息屏后热点无线电会掐断对方设备的 TCP 连接（close 码 1006），对方就显示未连接。现在选文件时会挂一个 1x1 透明悬浮窗（`FLAG_KEEP_SCREEN_ON`）强制保持屏幕常亮——屏幕不熄、热点不断，连接不再被掐。该方案需要**「显示在其他应用上层」权限**（`SYSTEM_ALERT_WINDOW`）：首次选文件时引导到系统设置开启一次（可跳过），开启后自动生效
- **短暂掉线不再导致对方「消失」**：即便没开上面权限、或连接仍有瞬断，客户端 1 秒内会自动重连；原来 1.5 秒的「断连提示宽限窗」太短，重连还没落地就把设备从列表里删了——对方看起来「直接消失」。现在宽限窗拉长到 5 秒，瞬断期间不清列表、保留 peer 状态，重连成功后无缝恢复；仅真正断线超过 5 秒才提示断开
- **服务端退出后，已连接手机立刻收到断开回应**：以前服务器手机返回首页 / 退出服务后，另一台手机可能一直显示「已连接」——服务端进程被强制结束时，热点链路常不会把 TCP 关闭（FIN）真正送到对方，客户端只能等系统 TCP 超时（几分钟甚至更久）。现在客户端每 3 秒向服务端发一次心跳 ping、服务端立即回 pong，**连续 9 秒收不到 pong 即判定服务端已消失**，立刻断开并清理设备列表（随后自动重连，服务端恢复时页面也自动恢复）。心跳同样计入服务端自己的保活预算，不会误清理健康设备
- **更新安装后自动清理安装包**：以前 App 内更新下载的 APK 在安装后会一直残留在应用私有下载目录（一个约 30~50MB），App 的「数据」体积随每次更新越变越大。现在下载完成时记下它对应的版本号，**新版本安装成功后，下次启动自动删除这份残留的更新包**（记录同时清除，绝不会误删后续下载的新包）
- **崩溃日志不会无限增长**：crash.log 超过 512KB 自动轮转成 `crash.log.old` 重新记录——崩溃循环也不会把 App 数据撑爆

### 新增
- **设置页新增「更新日志」**：从 GitHub 实时拉取并渲染 `updatelog.md`（Markdown，支持表格 / 删除线 / 可点击链接），始终展示最新版本说明，无需等更新弹窗
- **「更新日志」与「关于本软件」改用国内加速拉取**：两个页面读取 GitHub 文档（updatelog.md / README.md）时，不再只走 GitHub 直连，而是与「检查更新」同一组加速镜像源（ghfast.top / gh.ddlc.top / gh-proxy.com / ghproxy.net）自动依次尝试、GitHub 直连兜底——国内查看文档不用再等直连超时或挂代理
- **设置页新增「已授权的权限」**：集中查看本 App 所需的可管理权限（相机 / 通知 / 附近 WiFi 设备或位置 / 显示在其他应用上层 / 安装未知应用），每个权限带实时开关状态——开启走系统授权弹窗，关闭跳转系统设置（返回自动刷新）；常规权限（网络 / 前台服务 / 唤醒锁等）说明为安装时授予、无法单独关闭
- **「隐私与 SDK」页补充悬浮窗权限详细说明**：写明「显示在其他应用上层」的用途（选文件时保持屏幕常亮、防热点断连）、使用时机（仅选文件期间，1×1 透明空白悬浮层）、不读取不收集任何数据、如何开关、拒绝授权的影响，以及「不代表 App 会有广告悬浮窗」

### 优化
- **传输会话保活再加固**：传输页整个会话期间保持屏幕常亮（不再只靠选文件时的悬浮层）——收文件的手机在等对方选文件时，自己的屏幕不会熄灭、连接也不会被系统掐断；服务端保活预算从 30 秒放宽到约 **10 分钟**，并改用**协议级 ping**（WebSocket ping 帧由浏览器网络栈自动应答、不跑页面 JS）——主机藏在系统文件选择器后面好几分钟，健康连接也不会被误清理

### 变更
- **「已授权的权限」升级为独立页面**：从设置页的弹窗改为一个完整页面，逐项列出本 App 的可管理权限（相机 / 通知 / 附近 WiFi 设备或位置 / 显示在其他应用上层 / 安装未知应用），每个权限附带**详细的隐私说明**——用途、什么时候读取了什么、拒绝授权的影响，并支持**实时开关**：开启走系统授权弹窗，关闭跳转系统设置（返回后自动刷新开关状态）；常规权限（网络 / 前台服务 / 唤醒锁等）在页面顶部说明为安装时授予、无法单独关闭
- **「隐私与 SDK」页全面改版**：新增「我们不收集、不共享任何数据」「数据安全与你的权利」两个章节；本机数据清单补充崩溃日志（crash.log）与更新安装包；SDK 与开源组件版本清单更新（express 4.22.2 / ws 8.21.1 / ua-parser-js 1.0.41 / unique-names-generator 4.7.1 / express-rate-limit 7.5.1 / NoSleep 0.12.0 / qrcode-svg 1.1.0 / nodejs-mobile 18.20.4）

## [1.0.89 ~ 1.0.102] - 2026-08-03

### 新增
- **首页网络状态识别**：自动识别当前网络——WiFi（含网络名）、移动数据、个人热点，并显示当前所用网络对应的 IP；切换网络后自动更新
- **首页网络开关**：WiFi / 移动数据 / 个人热点 合成一行（系统样式图标），可直接开启 / 关闭——开启绿色、关闭深灰；系统禁止 App 直接切换时自动打开对应系统面板
- **启动按钮智能提示**：按当前网络提示对方如何连上本机——WiFi 下提示「连接同一 WiFi 才能进行传输」，移动数据 / 个人热点下提示「需要连接本台设备的热点」
- **全 App 浅色主题**：首页、传输页顶栏与所有子页统一改为浅灰（#ECEFF1）主题（此前仅设置页是浅色），系统栏图标同步由白色改为深色

### 变更
- **「隐私与 SDK」页更新**：补充首页网络开关新增的权限（修改 WiFi / 网络状态、附近 WiFi 设备 / 位置）说明，并明确 WiFi 网络名仅用于首页显示、不存储不上传

### 优化
- **局域网地址小屏自适应**：地址字号随屏幕自动缩放（12~20sp），小屏不溢出

### 修复
- **纯局域网 / 热点（无外网）也能配对和建公共房间**：之前 WebSocket 连接会被 `navigator.onLine` 误判为离线而拦下，手机只开热点、没有任何互联网时会提示「需要连接到互联网才能配对 / 创建公共房间」；现在改为始终连接本机服务器（服务器本就在本地跑），无外网也能正常配对、创建房间、互传文件，也不再弹「已离线」提示

## [1.0.85 ~ 1.0.88] - 2026-08-03

### 安全
- **扫码连接只认局域网服务器二维码**：以前任意网站（https 或公网 IP）的二维码扫码后都会被 App 打开，现在只接受 lan-projects 服务器的 http 连接码 / 配对码 / 房间码（私网 IP、IPv6 本地、`.local` 等局域网主机），其余一律提示「无法识别」——恶意二维码再也无法把 App 导向钓鱼网站
- **恶意网页无法借 App 写文件**：即使有非局域网页面混进 WebView，其 JS 桥（`LanProjectsBridge`）与本地保存接口都会因**来源校验**被拒绝，无法向「下载」目录投放伪造的安装包或文件
- **公网页面不再出现在 App 内**：WebView 遇到指向公网的链接 / 重定向会**自动改用系统浏览器打开**，并兜底拦截服务端 302 重定向，非局域网页面永远进不了 App 的 WebView 主框架
- **最近连接列表安全清理**：点按前重新校验历史记录，早前误存的异常网址自动移除

### 变更
- **「隐私与 SDK」页更新**：明确说明「检查更新 / 下载更新包」会经第三方加速代理（ghfast.top / gh.ddlc.top / gh-proxy.com / ghproxy.net）转发 GitHub，文件可能经第三方服务器中转；并重新核对 SDK 版本清单

### 优化
- **更新说明 Markdown 渲染升级**：与「关于本软件 / 隐私与 SDK」页同款渲染，支持表格、删除线、可点击链接；弹窗内容高度受限，长更新日志不再顶掉「下载并安装」按钮

## [1.0.76 ~ 1.0.85] - 2026-08-02

### 变更
- **首页全新深色设计**：与全 App 主题统一（深蓝灰渐变背景、连接状态卡片、半透明胶囊按钮），本机地址更醒目；首页不再展示应用图标
- 首页新增**局域网连接状态提示**（已连接绿点 / 未连接灰点）与**复制地址**按钮：复制出的是一整条地址 `http://IP:3000`，可直接粘贴到浏览器打开，或发给同一局域网的朋友

### 新增
- **首页下拉刷新局域网地址**：在页面顶部下拉，页面随手指下移并出现转圈图标，松手即重新读取局域网地址——切换 WiFi 后不用重启 App
- **更新下载国内加速**：检查更新与 APK 下载不再只走 GitHub 直连，而是自动依次尝试多个加速镜像源（ghfast.top / gh.ddlc.top / gh-proxy.com / ghproxy.net），GitHub 直连兜底——国内连 GitHub 慢或不通时自动切到可用镜像，无需任何手动设置
- **镜像源之间断点续传**：某个源中途失败会自动换下一个源接着下，已下载部分不丢失
- GitHub 发布页附带「国内加速下载」链接，浏览器手动下载也可走加速通道

### 修复
- **服务器运行中，服务通知栏无法删除**：Android 14+ 允许划掉常驻通知，现在即使被划掉也会**立即重新出现**——只有停止服务（返回首页）通知才会消失

### 优化
- **进入传输页提速**：服务器启动前的旧进程清理从「最长等 8 秒」改为毫秒级本地端口探测，重复进出明显更快
- WebView 关闭 SafeBrowsing 黑名单查询（局域网页面加载更快、更隐私），并开启离屏预栅格（首帧绘制更快）
- 局域网地址字体缩小（26sp → 20sp），完整地址一行放下
- 崩溃日志记录 **apk 版本号 + 进程名 + 线程名**，出现问题可直接定位到具体版本与代码路径

## [1.0.75] - 2026-08-02

### 优化
- **日志瘦身**：手机端日志只保留**最近 100 条**，超出的旧行自动删除（Java 与 Node 两侧共同裁剪），文件不再无限增长
- 移除重复日志：同一断开事件不再记两条（删掉 SOCKET-CLOSE 冗余行）
- 保存文件时不再递归扫描目录、不再打印大文件 / 缓存详情日志（老诊断逻辑，纯噪音且白费性能）

### 新增
- 「隐私与 SDK」改为**独立详细页面**：离线渲染，含**隐私政策**（数据不出局域网、端到端加密）、**本机数据清单**、**权限用途**、**完整 SDK 与开源组件清单**（Android SDK / WebView / nodejs-mobile / AndroidX / ZXing / Markwon / npm 依赖 / 前端库，逐项标注许可证）

## [1.0.72 ~ 1.0.74] - 2026-08

### 说明
- 版本号递增的**重新发布**（功能与 v1.0.71 一致，无新功能）

## [1.0.71] - 2026-08-02

### 变更
- 全 App **浅灰主题化**（设置 / 关于 / 日志 / 测速 / 启动页）
- 设置项改为**左对齐**；**版本号 + GitHub 图标固定在页面底部**（不随列表滚动）
- 启动页 UI 优化：局域网 IP 卡片、最近连接浅色卡片
- **最近连接持久化生效**：连接过的设备记录在本地，回首页仍在、点按可一键重连

## [1.0.68] - 2026-08-02

### 变更
- 「关于本软件」改为**独立页面**：实时从 GitHub 加载并渲染 README（Markdown）
- 设置页**版本号移到页面底部**，其下放 **GitHub 图标**（点击跳转仓库）
- 移除设置页顶部的版本号行与「开源项目 GitHub」行

## [1.0.67] - 2026-08-02

### 变更 / 修复
- 「检查更新」**仅官方签名包可用**：debug 或换签名的克隆包无法使用（非官方提示"不支持检查更新"）
- 更新说明以 **Markdown 格式**呈现在 App 内（标题 / 列表 / 加粗等）
- 下载改为**前台通知**：通知栏显示实时进度、**不可划掉**，支持**暂停 / 继续 / 取消**，下载完成自动调起安装器

## [1.0.64] - 2026-08-02

### 新增
- 设置页新增「**检查更新**」：自动查询 GitHub 最新版本，发现新版可一键下载并安装

## [1.0.63] - 2026-08-02

### 修复
- 接收大文件后 App 体积膨胀（Android WebView 把接收文件的 Blob 持久化到磁盘 `app_webview/Default/blob_storage` 且不自动清理）：现在**关闭传输页时自动清理**，回到首页时兜底再清一次
- 下载后不再创建永不释放的对象 URL，修复浏览器 / WebView 内存随接收文件膨胀

### 优化
- 加密改用 ChaCha20（纯 JS，比纯 JS AES 快约 12 倍），大文件传输不再卡顿

## [1.0.53] - 2026-08-02

### 变更
- 首页简化：移除「连接其他设备」，保留「📱 用本设备作为启动」+「📷 扫码连接」
- 扫码连接支持三种二维码：**连接码 / 配对码 / 房间码**
- 修复扫码页返回键被状态栏遮挡

## [1.0.52] - 2026-08-02

### 变更
- **端到端加密升级**：密钥从配对秘密派生（线下扫码 / 配对码建立），**未配对设备拒绝传输**
- 加密方式由可公开推导的 XOR 流密码改为强加密（ChaCha20）

## [1.0.51] - 2026-08-02

### 修复
- 修复主机（服务端）收不到客户端文件：IP 房间统一为**服务器局域网地址**，收发双方加密密钥一致

## [1.0.34 ~ 1.0.50] - 2026-08

### 迭代
- 持续功能迭代：设置页、速度测试、WebSocket 二进制帧传输提速、大文件原生写盘（SaveFileServer）等

## [1.0.0] - 2026-07-27

### 新增
- lan-projects 初始版本发布
- 基于 WebRTC 的点对点文件共享
- 局域网设备发现
- 临时公共房间支持互联网传输
- 通过 6 位配对码或二维码绑定设备
- 跨平台支持（桌面、平板、手机）
- 30+ 语言支持
- Web Share Target 集成
- 服务工作线程离线缓存
- 端到端加密传输
