node_modules/
package-lock.json
*.log
.env
.DS_Store
Thumbs.db

# Build artifacts: APKs and release packages are distributed via GitHub
# Releases, not committed to the repo.
*.apk
*.zip

# Backup files never belong in the repo. SDK package.xml originals are
# kept at <parent>/\sdk-package-xml-backups-<date>\ (outside the repo).
*.bak
